class CartModel
{
  String name;
  String picture;
String price;
  String size;
  String color;
 String quantity;

  CartModel(this.name, this.picture, this.price, this.size, this.color,
      this.quantity);
}